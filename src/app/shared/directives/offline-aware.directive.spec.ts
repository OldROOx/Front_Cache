import { OfflineAwareDirective } from './offline-aware.directive';

describe('OfflineAwareDirective', () => {
  it('should create an instance', () => {
    const directive = new OfflineAwareDirective();
    expect(directive).toBeTruthy();
  });
});
